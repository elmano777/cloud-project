# Build stage
FROM golang:1.21 AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o main .

# Run stage
FROM debian:bullseye-slim
WORKDIR /app
COPY --from=builder /app/main .

EXPOSE 3000
CMD ["./main"]
